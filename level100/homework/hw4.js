//მომხმარებელს შეეკითხე ქულა (0–100).

//თუ ქულა < 50 → "ჩაიჭერი".

//თუ 50–69 → "საშუალო".

////თუ 70–89 → "კარგი".

//თუ 90–100 → "ძალიან კარგი".

//თუ სხვა რიცხვი შეიყვანა → "არასწორი მონაცემი".

let point = Number(prompt("გთხოვთ შეიყვანოთ ქულა 0-100მდე:"))

if (point < 0 || score > 100 || isNaN(point)) {
  console.log("არასწორი მონაცემი")
} else if (point < 50) {
  console.log("ჩაიჭერი")
} else if (point <= 69) {
  console.log("საშუალო")
} else if (point <= 89) {
  console.log("კარგი")
} else {
  console.log("ძალიან კარგი")
}

